[project]
name = "agent-profiles"
version = "0.1.0"
description = "Build portable developer roles from versioned specialist skill packs"
requires-python = ">=3.11"
dependencies = ["PyYAML==6.0.3"]

[tool.uv]
package = false
